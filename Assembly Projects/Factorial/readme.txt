This program takes as input an integer, n, and returns as a result n factorial.

To compile, use `gcc -o factorial factorial.S -g`.

`./factorial` will run the compiled program.

The C file has some error handling.
This program takes as input two integers, first the base, then the exponent, and returns as a result base to the power of exponent.

To compile, use `gcc -o power power.S -g`.

To run, use `./power`.

The C file has some error handling.
